namespace Assessment1_practice;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, World!");
    }

    //Create a static method named IsPassing() that returnns a bool and takes in a grade (double) as a parameter
    static bool IsPassing(double grade)
    {
        if (grade > 65)
    }

    {
        return true;
    }
     else 
            (grade <= 65)
        
        

        
            //Create a static method named AverageGrades() that takes in 3 parameters,all doubles

            static bool AverageGrade(double grade1, double grade2, double grade3)
                //This method should return the averages of all grades
            {
                return AverageGrade(grade1, grade2, grade3);
            }
            //create a static method OddOrEvenAndPassing() takes in a grade(double) as a parameter.
            //Convert the grade to an int and run it through the following
            //Need to Reuses the IsPassing() method here:
            // if grade is passing an odd / return "Passing and odd"
            //if the grade is failing and odd/ return"Failing and odd"
            //if the grade is passsing and even/ return "passing and even"
            // if the grade is failing and even/ return " failing and even"

            static string OddOrEvenAndPassing(double grade);
            // int intgrade = Convert.ToInt32(grade);
           // int intgrade = (int)grade;

           // bool isPassing = IsPassing(intgrade);

            if (grade = double)
            {
                return "   
            }
            //else if





    

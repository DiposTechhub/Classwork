namespace MockAssesssment02;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello, Welcome to Star Wars Character!");

        //Create a static method named AddStarWarsCharacter() that take in a string[]
        // A. This method searches for and indexof Yoda
        // b.   "Yoda" is not in array, return -1.

        static string AddStarWarsCharacters(string[] StarWarsCharacters)
        {
            for (int i = 0; i < StarWarsCharacters.length; i++)
            {
                return i;
            }
            }
        return;-1
                
            }
            return
        }
            
        static string DeathStarCombat(Dictionary<string, int Jedidi>)
        {
        
        }
        
        
       
        
        // create a static method named DeathStarCombat90 that take in a 
       // dictionary<string,int> of starwars characters (key) and their attack (value)
       // a.This method should return the name of the jedi with the highest attack value.
       // Dictionary to hold name (key) and grade (value)
       //         key     value
       //         type    type          name      = new Dictionary<key-type, value>();

       static string DeathStarCombat(Dictionary<string, int> charscore)
       {
           
           
           
           
           
       }


       
       string userResponse = "";
       do
       {
           
       }
       
       //Create a static method named ConvertPlanets() takes in one parameter: an array of planets names
       //  a. The method will convert the array of planets into a lit of planets in reverse order
       //  b. Return List
       
         static string  ConvertPlanets(string[] planet)
         